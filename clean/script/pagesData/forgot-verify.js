import React from "react";
import ForgotForm from "../containers/Forgot/Verify";

const ForgotVerify = (props) => (
  <React.Fragment>
    { (
      <ForgotForm {...props} />
    )}
  </React.Fragment>
);

export default ForgotVerify;
