import React from "react";
import ChannelView from "../containers/Channel/Index";

const Channel = (props) => (
  <React.Fragment>
    <ChannelView {...props} />
  </React.Fragment>
);

export default Channel;
