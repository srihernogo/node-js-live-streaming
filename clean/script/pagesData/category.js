import React from "react";
import CategoryView from "../containers/Category/Index";

const Category = (props) => (
  <React.Fragment>    
      <CategoryView {...props} />
  </React.Fragment>
);

export default Category;
