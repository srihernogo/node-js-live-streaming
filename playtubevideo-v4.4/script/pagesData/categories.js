import React from "react";
import CategoryBrowse from "../containers/Category/Browse";

const Category = (props) => (
  <React.Fragment>
      <CategoryBrowse {...props} />
    
  </React.Fragment>
);

export default Category;
