import React from "react";
import UpgradeView from "../containers/Upgrade/Index";

const Upgrade = (props) => (
  <React.Fragment>
    <UpgradeView {...props} />
  </React.Fragment>
);

export default Upgrade;
