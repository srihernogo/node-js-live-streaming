import React from 'react';
import PageNotFound from "../containers/Error/PageNotFound"


const Index = (props) => (  
  <React.Fragment>
    <PageNotFound {...props} />
  </React.Fragment>
)

export default Index