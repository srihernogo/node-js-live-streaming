import React from 'react';
import Maintenance from "../containers/Error/Maintenance"
const Index = (props) => (
  <React.Fragment>
      <Maintenance {...props} />
  </React.Fragment>
)

export default Index